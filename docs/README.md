# Documentation

Design notes and lab documentation for ShellCraft.

Planned contents:

- `design.md` — architecture of the shell: input loop, parser, executor, job table
- `syscalls.md` — notes on the POSIX system calls used and why
- `milestones.md` — what was implemented at each stage, with observations

These documents will be added as the corresponding features are implemented.
