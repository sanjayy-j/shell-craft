# Tests

Manual test scripts and sample inputs used to verify each ShellCraft milestone
(command execution, redirection, pipes, background jobs, job control, signals).

Tests will be added alongside the features they cover.
